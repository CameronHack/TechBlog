{{#each posts as |post| }}
<div class="post">
  <div class="">
    <h2>
      <a href="/post/{{post.id}}">{{post.name}}</a>
    </h2>
    <p>Created by {{post.user.name}} on {{format_date post.date_created}}</p>
  </div>
  <div class="">
    <p>
      {{post.description}}
    </p>
  </div>
</div>
{{/each}}